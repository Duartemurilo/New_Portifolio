export const goToHomePage = (history: any) => {
  history(`/`);
};

export const goToAboutPage = (history: any) => {
  history(`/about`);
};

export const goToProjectsPage = (history: any) => {
  history(`/projetos`);
};

export const goToContactPage = (history: any) => {
  history(`/contato`);
};
